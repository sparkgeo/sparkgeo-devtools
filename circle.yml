general:
    branches:
        only:
            - master
            - dev

machine:
    services:
        - docker

dependencies:
    pre:
        - pip install -U awscli boto3 setuptools
    post:
        - sparkgeo --help


#deployment:
#    prod:
#        branch: master
#        commands:
#            - Push to pypi